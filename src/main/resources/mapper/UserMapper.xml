<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.travel.mapper.UserMapper">

    <!-- 이메일 중복 확인 -->
    <select id="existsByEmail" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM tusers
        WHERE email = #{email}
    </select>

    <!-- 회원가입 처리 -->
    <insert id="insertUser" parameterType="com.example.travel.dto.UserDto" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO tusers (email, password, nickname, created_at)
        VALUES (#{email}, #{password}, #{nickname}, NOW())
    </insert>

    <!-- 이메일로 회원 조회 -->
    <select id="findByEmail" parameterType="string" resultType="com.example.travel.dto.UserDto">
        SELECT user_id AS userId, email, password, nickname, created_at AS createdAt
        FROM tusers
        WHERE email = #{email}
    </select>

</mapper>